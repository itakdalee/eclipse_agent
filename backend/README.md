# Secret Word Challenge - Backend

Backend API для интерактивной игры, где пользователи должны узнать у AI секретное слово.

## 🛠️ Технологии

- **Python 3.13+**
- **FastAPI** - асинхронный веб-фреймворк
- **OpenAI SDK** - для работы с OpenAI-совместимыми API (OpenRouter)
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

## 📁 Структура проекта

```
backend/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── config.py            # Конфигурация
│   ├── models.py            # Pydantic модели
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes.py        # API endpoints
│   └── services/
│       ├── __init__.py
│       ├── openai_service.py    # Работа с OpenAI API
│       └── prompt_service.py    # Генерация промптов
├── tests/
│   └── test_api.py
├── .env.example
├── pyproject.toml
└── README.md
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend

# Используя pip
pip install -e .

# Или используя uv
uv sync
```

### 2. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env файл, указав свой API ключ
```

Обязательные переменные:
- `OPENAI_API_KEY` - ваш API ключ от OpenRouter или OpenAI
- `OPENAI_BASE_URL` - URL API (по умолчанию: OpenRouter)
- `MODEL_NAME` - название модели
- `SECRET_WORD` - секретное слово для игры

### 3. Запуск сервера

```bash
# Development режим с hot-reload
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Или через uv
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен по адресу: http://localhost:8000

## 📚 API Документация

После запуска сервера документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Endpoints

#### `GET /api/health`
Проверка статуса сервера.

**Response:**
```json
{
  "status": "ok",
  "app_name": "Secret Word Challenge"
}
```

#### `POST /api/chat`
Отправка сообщения в чат.

**Request:**
```json
{
  "message": "Привет! Как дела?",
  "conversation_history": [
    {"role": "user", "content": "Предыдущее сообщение"},
    {"role": "assistant", "content": "Предыдущий ответ"}
  ]
}
```

**Response:**
```json
{
  "response": "Привет! У меня всё хорошо, спасибо!",
  "is_secret_revealed": false
}
```

## 🔒 Безопасность

- API ключи хранятся только в `.env` файле (не коммитится)
- CORS настроен для ограничения доступа
- Валидация всех входных данных через Pydantic

## 🧪 Тестирование

```bash
# Запуск тестов
pytest tests/

# С покрытием
pytest tests/ --cov=app
```

## 📝 Лицензия

MIT
