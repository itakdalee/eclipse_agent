# 🔐 Secret Word Challenge

Интерактивная игра, где пользователи должны узнать у AI секретное слово, используя социальную инженерию. Нейросеть защищена системным промптом, который запрещает раскрывать секрет.

## 📋 Содержание

- [Технологии](#-технологии)
- [Структура проекта](#-структура-проекта)
- [Быстрый старт](#-быстрый-старт)
- [Конфигурация](#-конфигурация)
- [API документация](#-api-документация)

---

## 🛠️ Технологии

### Backend
- **Python 3.13+**
- **FastAPI** - асинхронный веб-фреймворк
- **OpenAI SDK** - для работы с OpenAI-совместимыми API (OpenRouter)
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **HTML5 + CSS3 + Vanilla JavaScript**
- **LocalStorage API** - хранение истории чата

### Инфраструктура
- **Docker** + **Docker Compose**
- **Nginx** - веб-сервер для фронтенда и проксирование API

---

## 📁 Структура проекта

```
eclipse_agent/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # FastAPI приложение
│   │   ├── config.py            # Конфигурация
│   │   ├── models.py            # Pydantic модели
│   │   ├── api/
│   │   │   └── routes.py        # API endpoints
│   │   └── services/
│   │       ├── openai_service.py
│   │       └── prompt_service.py
│   ├── tests/
│   ├── pyproject.toml
│   ├── Dockerfile
│   └── .env.example
├── frontend/
│   ├── index.html
│   ├── css/
│   │   └── style.css
│   └── js/
│       ├── app.js
│       ├── api.js
│       └── storage.js
├── docker-compose.yml
├── nginx.conf
└── docs/
    └── PLAN.md
```

---

## 🚀 Быстрый старт

### Требования

- **Docker** 20.10+
- **Docker Compose** 2.0+

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd eclipse_agent
```

### 2. Настройка переменных окружения

```bash
# Скопировать пример конфигурации
cp backend/.env.example backend/.env

# Отредактировать .env файл
nano backend/.env
```

**Содержимое `.env`:**
```env
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://openrouter.ai/api/v1
MODEL_NAME=anthropic/claude-3.5-sonnet
SECRET_WORD=ECLIPSE2025
DEBUG=false
CORS_ORIGINS=["http://localhost","http://127.0.0.1"]
```

### 3. Запуск

```bash
# Сборка и запуск контейнеров
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f
```

Откройте http://localhost в браузере.

### Остановка

```bash
docker-compose down
```

---

## 🔄 Обновление приложения

```bash
# Получение обновлений
git pull origin main

# Пересборка и перезапуск
docker-compose up -d --build
```

---

## 📊 Мониторинг и логи

```bash
# Просмотр логов всех контейнеров
docker-compose logs -f

# Просмотр логов backend
docker-compose logs -f backend

# Просмотр логов frontend (nginx)
docker-compose logs -f frontend

# Статус контейнеров
docker-compose ps
```

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `OPENAI_API_KEY` | API ключ OpenRouter/OpenAI | *обязательно* |
| `OPENAI_BASE_URL` | URL API endpoint | `https://openrouter.ai/api/v1` |
| `MODEL_NAME` | Название модели | `anthropic/claude-3.5-sonnet` |
| `SECRET_WORD` | Секретное слово для игры | `ECLIPSE2025` |
| `DEBUG` | Режим отладки | `false` |
| `CORS_ORIGINS` | Разрешённые источники CORS | `["http://localhost"]` |

---

## 📚 API Документация

После запуска контейнеров документация доступна:

- **Swagger UI:** http://localhost/docs
- **ReDoc:** http://localhost/redoc

### Основные endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/health` | Проверка статуса сервера |
| `POST` | `/api/chat` | Отправка сообщения в чат |
| `POST` | `/api/reset` | Сброс сессии |

---

## 🔧 Troubleshooting

### Контейнеры не запускаются

```bash
# Проверка логов
docker-compose logs

# Проверка переменных окружения
cat backend/.env

# Перезапуск с пересборкой
docker-compose down
docker-compose up -d --build
```

### Ошибка 502 Bad Gateway

```bash
# Проверка, работает ли backend
docker-compose exec backend curl http://localhost:8000/api/health

# Перезапуск backend
docker-compose restart backend
```

### CORS ошибки

Убедитесь, что `CORS_ORIGINS` в `.env` содержит правильный домен:
```env
CORS_ORIGINS=["http://localhost","https://yourdomain.com"]
```

---

## 📝 Лицензия

MIT License

---

## 👥 Авторы

Secret Word Challenge Team
